{
  "name": "warpwing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "WarpWing — an original N64-era inspired on-rails space shooter. Not affiliated with Nintendo.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
